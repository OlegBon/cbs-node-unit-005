{
  "name": "middleware",
  "version": "1.0.0",
  "description": "Create middleware to log requests from clients. The logging should write to a file all request parameters, request method, and IP address.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "middleware",
    "log",
    "express"
  ],
  "author": "OlegBon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "nodemon": "^3.1.9"
  }
}
